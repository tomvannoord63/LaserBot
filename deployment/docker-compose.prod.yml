services:
  frontend:
    build: 
      context: ../frontend
      dockerfile: Dockerfile.static
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
